



export function parseFilters(exp: string): string {
  let expression = "";
  let pos: number;

  for (pos = 0; pos < exp.length; pos++){

  }

  expression = exp.slice(0, pos);

  return expression;
}